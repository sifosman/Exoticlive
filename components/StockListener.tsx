'use client';

// This component has been disabled since we're now using a cron job for stock updates
export default function StockListener() {
  // Return an empty div (component is not used anymore)
  return null;
}
